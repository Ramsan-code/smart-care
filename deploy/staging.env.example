# Nonsecret settings. Secrets are injected as files, never added here.
RELEASE_TAG=local
ALLOWED_HOSTS=localhost
CSRF_TRUSTED_ORIGINS=https://localhost:8443
# TLS/key/database/health secret file locations: see compose.staging.yml.
# Production: DJANGO_SETTINGS_MODULE=smartcare.settings_production
# Staging: smartcare.settings_staging (financial integrations deliberately disabled).
# Isolated synthetic tests: compose.isolated.yml + separate project/volumes.
